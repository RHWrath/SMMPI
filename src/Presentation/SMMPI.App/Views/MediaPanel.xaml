<UserControl x:Class="SMMPI.App.Views.MediaPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewModels="clr-namespace:SMMPI.App.ViewModels">
    <UserControl.Resources>
        <Style x:Key="ThumbTile" TargetType="ListBoxItem">
            <Setter Property="Width" Value="112" />
            <Setter Property="Height" Value="128" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource Theme_SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme_BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Theme_AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#FFEFF6FF" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Theme_AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource PanelCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Style="{StaticResource SectionTitle}"
                           Text="Media"
                           Margin="0,0,0,12" />
                <Border Grid.Row="1"
                        Style="{StaticResource InnerSurface}"
                        Padding="8"
                        ClipToBounds="True">
                    <ListBox ItemsSource="{Binding MediaItems}"
                             SelectedItem="{Binding SelectedMedia}"
                             ItemContainerStyle="{StaticResource ThumbTile}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewModels:MediaItemViewModel}">
                                <Grid Margin="6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="#FFE9EEF5" CornerRadius="6">
                                        <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" />
                                    </Border>
                                    <TextBlock Grid.Row="1"
                                               Text="{Binding FileName}"
                                               TextTrimming="CharacterEllipsis"
                                               FontSize="11"
                                               Margin="0,6,0,0" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource PanelCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0"
                           Style="{StaticResource SectionTitle}"
                           Text="Mediavoorbeeld"
                           Margin="0,0,0,10" />
                <Border Grid.Row="1"
                        Style="{StaticResource InnerSurface}">
                    <Grid>
                        <Image Source="{Binding SelectedPreview}"
                               Stretch="Uniform"
                               RenderOptions.BitmapScalingMode="HighQuality" />
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource Theme_MutedTextBrush}"
                                   Text="Voorbeeld geselecteerd bestand">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SelectedPreview}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>
                <Grid Grid.Row="2" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                        <TextBlock Foreground="{DynamicResource Theme_TextBrush}" Text="{Binding SelectedFileName, StringFormat=Geselecteerd: {0}}" />
                        <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource Theme_MutedTextBrush}" Text="{Binding SelectedFileType, StringFormat=Type: {0}}" />
                    </StackPanel>
                    <Button Grid.Column="1"
                            Content="Naar telefoon sturen"
                            Command="{Binding SendToDeviceCommand}"
                            MinWidth="120"
                            VerticalAlignment="Center"
                            Margin="12,0,0,0" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
