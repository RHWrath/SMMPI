<Application x:Class="SMMPI.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Application.Resources>
        <Color x:Key="Theme_BackgroundColor">#FFF6F8FB</Color>
        <Color x:Key="Theme_SurfaceColor">#FFFFFFFF</Color>
        <Color x:Key="Theme_SurfaceAltColor">#FFF9FAFC</Color>
        <Color x:Key="Theme_BorderColor">#FFD7DEE8</Color>
        <Color x:Key="Theme_AccentColor">#FF2563EB</Color>
        <Color x:Key="Theme_AccentHoverColor">#FF1D4ED8</Color>
        <Color x:Key="Theme_TextColor">#FF172033</Color>
        <Color x:Key="Theme_MutedTextColor">#FF667085</Color>
        <SolidColorBrush x:Key="Theme_BackgroundBrush" Color="{DynamicResource Theme_BackgroundColor}" />
        <SolidColorBrush x:Key="Theme_SurfaceBrush" Color="{DynamicResource Theme_SurfaceColor}" />
        <SolidColorBrush x:Key="Theme_SurfaceAltBrush" Color="{DynamicResource Theme_SurfaceAltColor}" />
        <SolidColorBrush x:Key="Theme_BorderBrush" Color="{DynamicResource Theme_BorderColor}" />
        <SolidColorBrush x:Key="Theme_AccentBrush" Color="{DynamicResource Theme_AccentColor}" />
        <SolidColorBrush x:Key="Theme_AccentHoverBrush" Color="{DynamicResource Theme_AccentHoverColor}" />
        <SolidColorBrush x:Key="Theme_TextBrush" Color="{DynamicResource Theme_TextColor}" />
        <SolidColorBrush x:Key="Theme_MutedTextBrush" Color="{DynamicResource Theme_MutedTextColor}" />

        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource Theme_BackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="PanelCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme_SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme_BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18"
                                      Direction="270"
                                      Opacity="0.08"
                                      ShadowDepth="2"
                                      Color="#FF111827" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InnerSurface" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource Theme_SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme_BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="8" />
        </Style>

        <Style x:Key="SectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{DynamicResource Theme_MutedTextBrush}" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="Background" Value="{DynamicResource Theme_SurfaceAltBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme_BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CaretBrush" Value="{DynamicResource Theme_TextBrush}" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Theme_AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#FFF3F6FA" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Foreground" Value="{DynamicResource Theme_TextBrush}" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="14,7" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{DynamicResource Theme_AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource Theme_AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource Theme_AccentHoverBrush}" />
                                <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource Theme_AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.86" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
